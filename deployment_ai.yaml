apiVersion: v1
kind: Service
metadata:
  name: personacanvas-frontend-service

spec:
  type: NodePort

  selector:
    app: personacanvas-frontend

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30081
